var RoomMapGenerator = function(room, spawn) {
    this.room = room;
    this.spawn = spawn;
    this.createPlans = function() {
        let mapPlans = {};
        //do stuff
        return mapPlans;
    }

    this.generateCurrentMap = function() {
        let curMap = {}; //contains current map, which maps out source locations, any harvesters assigned to them, any transport creep stations, extensions, spawns, roads, etc.
        //do stuff
        return curMap;
    }

}